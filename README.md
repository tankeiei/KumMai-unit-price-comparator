# 🛒 แอป "คุ้มไหม?" — Unit Price Comparator

เครื่องมือเปรียบเทียบราคาต่อหน่วย (Unit Price Comparator) พัฒนาด้วย **React Native** ผ่าน **Expo (Managed Workflow)** ออกแบบมาเพื่อช่วยตัดสินใจซื้อสินค้าให้ได้ความคุ้มค่าที่สุด คำนวณรวดเร็ว พร้อมแสดงเปอร์เซ็นต์ความคุ้มค่าและจัดอันดับให้อัตโนมัติ สามารถรันผ่าน **Expo Go** บนมือถือได้ทันทีโดยไม่ต้อง Build Native Code

---

## 🚀 วิธีการรันโปรเจกต์แบบสรุป (Quick Start)

### 1. เปิด Terminal เข้าโฟลเดอร์โปรเจกต์ `value-comparator`

```bash
cd value-comparator
```

### 2. ติดตั้ง Dependencies

```bash
npm install --legacy-peer-deps
```

### 3. รันเซิร์ฟเวอร์พัฒนา (Expo Dev Server)

```bash
npx expo start
```

### 4. เปิดใช้งานบนมือถือ
- เปิดแอป **Expo Go** บน iOS/Android แล้วสแกน **QR Code** ที่ขึ้นบน Terminal

---

ข้อมูลรายละเอียดเพิ่มเติมและโครงสร้างโปรเจกต์ ดูได้ที่ [value-comparator/README.md](file:///c:/Users/tanku/Desktop/Price%20calculator/value-comparator/README.md)
